[pytest]
# backend/ is the import root — the app's own code uses absolute imports
# like "from core.plan_guard import ..." and "from services.booking_service
# import ...", so tests need the same root on the path to import the real
# code rather than reimplementing it.
pythonpath = backend
testpaths = tests
python_files = test_*.py
